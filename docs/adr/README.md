# Architecture Decision Records

NullVector uses ADRs to capture non-trivial technical decisions and their consequences.

## Conventions

- Files are numbered sequentially.
- Titles describe the decision, not the discussion.
- Status values should be one of `proposed`, `accepted`, `superseded`, or `rejected`.
- Update or supersede an ADR instead of silently changing a previous decision.

