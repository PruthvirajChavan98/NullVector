## Mandatory runtime header

Before doing anything else:
- run `bash .codex/bin/preflight-codex.sh`
- if it fails, stop and report the failure

Before any architectural or multi-step reasoning:
- use the sequential-thinking MCP server first

Before any claim about current tools, APIs, models, packages, or platform behavior:
- perform web search first
- cite authoritative sources

If either required capability is unavailable:
- stop
- do not continue
- report the blocker verbatim

